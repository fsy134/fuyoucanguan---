# 福友餐馆 · 微信扫码点餐小程序 —— 开发计划书

## 一、背景与目标

餐厅堂食场景改造：每张餐桌贴一张**专属二维码**，顾客扫码后自动认出桌号 → 询问几位用餐 → 微信登录点餐 → 下单 → 催单/付款。商家后台实时收到"X号桌已下单/已支付"提醒。

**已有资产**（不动或小改）：
- Java 后端（苍穹外卖改造版，Spring Boot，本地 8080 端口）——点餐、购物车、下单、催单、模拟支付等接口大部分现成
- 商家管理端（Vue 项目）——商家看订单、收提醒用，本次不改

**要做的事**：在 `c:\Users\15122\Desktop\fuyoucanguan` 从零搭建**微信原生小程序**（现有目录是微信云开发空模板，业务代码全部新建）。

**已拍板的技术决策**：
1. 开发方式：微信原生开发（微信开发者工具直接开发）
2. 支付：先用后端现成的模拟支付（点"付款"即算付成功，钱不走微信，以后可升级真支付）
3. 桌号：走后端新增的 `tableNum` 字段（你自己已改了一半，剩余部分由我们在实现阶段补完，**改前逐条给你过目**）
4. 人数：只在小程序前端显示（"3号桌 · 4位"），不提交后端

## 二、技术架构讲解（通俗版）

整套系统就像一家餐馆的各个岗位：

| 部件 | 生活比喻 | 说明 |
|---|---|---|
| **微信小程序** | 顾客手里的"电子菜单本" | 顾客用它看菜单、点菜、下单、催单、付款 |
| **Java 后端** | 厨房 + 前台系统 | 接收点单、记账、算钱、通过"对讲机"喊厨房 |
| **MySQL 数据库** | 总账本 | 菜单、购物车、订单都记在这里 |
| **Redis** | 收银台小黑板 | 只记"营业中/打烊"这类要快速查的小信息 |
| **微信登录** | 微信代发"会员卡" | 顾客扫一下，微信给小程序发一张临时"会员卡"（叫 token，2 小时有效），之后点餐都凭这张卡，不用输入账号密码 |
| **WebSocket** | 厨房对讲机 | 顾客下单/付款/催单的瞬间，商家后台立刻弹出提醒 |
| **阿里云 OSS** | 公用图库 | 菜品的照片存在云端图库，小程序直接按链接显示 |

**一次完整点餐的数据流动**：

```
顾客扫码 → 小程序认出桌号 → 问几位用餐 → 微信静默登录(换"会员卡"token)
→ 逛菜单(后端从账本取菜单) → 加购物车(记在后端账本)
→ 点"下单"(后端记账、开新订单，并通过对讲机喊商家"3号桌已下单")
→ 点"付款"(模拟支付，订单变"已完成") → 随时可点"催单"(对讲机再喊一次)
```

## 三、现状核实结果（侦察发现，务必知晓）

对你的后端逐行核对后的结论：

**✅ 你已经改好的**：`Orders` 实体已有 `tableNum`（桌号）字段；下单/付款时的商家提醒已带上"X号桌已下单/已支付"。

**⚠️ 还剩 5 处必须补完**（见第四节），其中两处是"必崩"级——现在下单、付款会直接报错，小程序做完也跑不通。

**🔴 新发现——订单状态编号对不上**：后端现在用「1已下单 / 2已完成 / 3已取消」，但你的商家后台是按「1待付款 / 5已完成 / 6已取消」显示的。不修的话：顾客付款后，商家后台看到的还是"待接单"状态，永远看不到"已完成"。必须改。

**✅ 澄清一个误会**：之前担心"用户取消订单"缺配置会崩——复查后发现它已通过注解方式实现，**不用改**。

**ℹ️ 顺带告知**：商家后台还有 4 个旧按钮（接单/派送/拒单/统计）连的是已删除的接口，点了会报错。不影响本次堂食主流程，以后可顺手隐藏，本次不动。

## 四、后端补完清单（共 5 处小改动）

全部集中在 3 个文件里。实现阶段**先逐条列出具体改动内容给你过目，你点头才动手**：

| # | 文件 | 改什么 | 为什么（不改会怎样） |
|---|---|---|---|
| 1 | `...\sky-pojo\...\entity\Orders.java` | 状态常量改为 `Ordered=1, COMPLETED=5, CANCELLED=6` | 与商家后台对齐，否则付款后商家看不到"已完成" |
| 2 | `...\sky-pojo\...\dto\OrdersSubmitDTO.java` | 增加 `private String tableNum;` | 小程序下单时把桌号传给后端落库（复制对象时会自动带上） |
| 3 | `...\sky-server\...\mapper\OrderMapper.xml` insert 语句 | 列清单精简为与实体一致：`number, status, user_id, order_time, checkout_time, pay_method, pay_status, amount, remark, phone, user_name, table_num, cancel_reason, cancel_time` | 现语句引用了 10 个已被删掉的旧字段，**下单必崩**；同时补上 table_num 列 |
| 4 | 同文件 update 语句 | 删掉 3 个引用已删字段的 if 判断（rejectionReason/deliveryStatus/deliveryTime） | 否则**付款、商家取消/完成订单都必崩** |
| 5 | 数据库 orders 表（验证即可） | 确认有 `table_num` 列；若没有则加一列 `table_num VARCHAR(20)` | 桌号要能存进账本；你说已改库，实现时验证一下 |

**不改的**：用户取消订单（已有注解实现）；购物车去重逻辑（现成）。

## 五、小程序设计方案

### 5.1 目录结构（保留现有 miniprogram/ 布局，清理云开发模板）

```
fuyoucanguan\
├── project.config.json      # 改：appid、关闭域名校验、加调试编译模式
└── miniprogram\
    ├── app.js               # 重写：解析扫码桌号 + 静默登录（全局枢纽）
    ├── app.json             # 重写：注册页面、窗口样式
    ├── app.wxss             # 重写：全局样式
    ├── config.js            # 新增：店铺名称/logo/营业信息、后端地址、桌数
    ├── utils\
    │   ├── request.js       # 新增：HTTP 封装（自动带会员卡 token、401 自动重登）
    │   └── util.js          # 新增：金额格式化、口味拼接
    ├── components\
    │   ├── flavor-popup\    # 新增：口味+数量弹窗
    │   └── cart-ball\       # 新增：右下角购物车球
    ├── images\              # 店铺 logo 等
    └── pages\
        ├── table\index      # 选桌页（没扫码直接打开小程序时的兜底入口）
        ├── menu\index       # 点餐页（核心：顶部店铺信息+左分类+右菜品+购物车球）
        ├── confirm\index    # 下单确认页（清单/备注/继续点餐/下单）
        ├── order\detail     # 订单详情页（状态/催单/付款/取消）
        ├── order\history    # 历史订单页
        └── closed\index     # 打烊提示页
```

清理动作：删除 `cloudfunctions/`、`pages/example/`、示例组件和云初始化代码。不使用底部 tabBar（点餐是单向流程，用不上；历史订单入口放在点餐页顶部）。

### 5.2 页面 × 接口对照表

| 页面/动作 | 调用接口 | 说明 |
|---|---|---|
| 全局启动 | POST `/user/user/login`（传 wx.login 的 code） | 返回"会员卡"token 存本地，之后所有请求自动带上 |
| 点餐页加载 | GET `/user/shop/status` | 免登录；返回 0 或异常 → 跳到打烊页 |
| 点餐页加载 | GET `/user/category/list?type=1` 和 `?type=2` | 两次并发；菜品分类+套餐分类合并，按 sort 排序显示在左侧 |
| 点左侧分类 | GET `/user/dish/list?categoryId=`（菜品分类） | 右侧显示菜品列表（图、价、加号） |
| 点左侧分类 | GET `/user/setmeal/list?categoryId=`（套餐分类） | 右侧显示套餐列表 |
| 点套餐加号 | GET `/user/setmeal/dish/{id}` | 弹窗展示套餐内含哪些菜（名称×份数） |
| 口味弹窗确认 | POST `/user/shoppingCart/add` | 菜品传 `{dishId, dishFlavor}`；套餐传 `{setmealId}`；要 N 份就连续调 N 次（后端每次 +1，同口味自动合并成一行） |
| 每次增删后 | GET `/user/shoppingCart/list` | 刷新购物车球徽标（总件数）和合计金额 |
| 下单页减一 | POST `/user/shoppingCart/sub` | 参数与 add 完全一致；数量 1 时删行 |
| 下单页清空 | DELETE `/user/shoppingCart/clean` | 二次确认后调用 |
| 点"下单" | POST `/user/order/submit` | 传 `{payMethod:1, remark:备注, amount:合计, tableNum:桌号}`；后端自动清空购物车 |
| 订单页加载 | GET `/user/order/orderDetail/{id}` | 显示状态/单号/金额/明细 |
| 催单 | GET `/user/order/reminder/{id}` | 调完提示"已催单，商家正在处理" |
| 付款 | PUT `/user/order/payment` | 传 `{orderNumber, payMethod:1}`；模拟支付，看返回 code=1 即成功 |
| 取消订单 | PUT `/user/order/cancel/{id}` | 二次确认 |
| 历史订单 | GET `/user/order/historyOrders?page&pageSize&status` | 分页加载；状态筛选 |
| 再来一单 | POST `/user/order/repetition/{id}` | 先清空购物车再调，避免混行 |

### 5.3 核心流程设计（关键规则）

**① 扫码认桌**：每张桌贴一张"小程序码"（微信官方接口生成，码里藏着桌号 scene=3）→ 小程序启动时解析出"3"存手机本地 → 进入点餐页。兜底：顾客直接打开小程序（没扫码）时先进入**选桌页**手动点桌号。

**② 人数弹窗**：进入点餐页时弹"请问几位用餐"（1~12 人），只存本地显示在页面顶部（"3号桌 · 4位"），不传后端（按你的要求）。

**③ 微信登录（静默无感）**：顾客全程不用点"授权"按钮。流程：小程序自动 wx.login 拿 code → 换 token 存本地 → 之后所有请求自动带 `authentication` 头。token 2 小时过期或失效时，自动重新登录并重试一次请求，顾客无感。

**④ 口味选择（重要规则）**：
- 点带口味菜品的"+"→ 弹出口味弹窗（如"辣度：不辣/微辣/中辣"+"温度：热饮/常温"），每个维度默认选第一个，配数量
- **口味拼接规则**：按口味定义顺序拼成字符串 `辣度:微辣,温度:热饮`，作为该购物车行的"身份证"——后端靠这个字符串区分"微辣"和"中辣"是两行。加购和减购必须传一模一样的字符串（直接用购物车行里的原值回传，不重新拼）
- 无口味的菜品点"+"直接加一份；套餐弹窗只选数量
- 注意：口味名和选项值里不要含 `:` 和 `,`（后台录菜单时注意）

**⑤ 购物车球**：右下角悬浮球，小红标显示总件数（各购物车行数量之和），点击进入下单确认页。下单页里每行可 +/- 调整、可清空、可填备注，底部显示合计金额（用"分"累加避免小数误差），两个按钮："继续点餐"返回、"下单"提交。

**⑥ 下单后**：跳订单详情页，显示订单号、金额、状态。待付款时显示"去付款"+"取消订单"；付款成功后状态变"已完成"；"催单"按钮任何状态都显示，点了商家后台立刻弹提醒。

**⑦ 打烊拦截**：进点餐页先查营业状态，打烊 → 跳到打烊提示页；下单前再查一次兜底。

### 5.4 工程细节

- **config.js 集中配置**：后端地址（开发期 `http://localhost:8080`；真机联调改成电脑局域网 IP `http://192.168.x.x:8080`；上线改 HTTPS 域名）；店铺名称/logo/营业信息写死在前端（商家后台没有店铺设置页，以后可升级为后端接口）
- **request.js 封装**：统一拼地址、带 token、处理 401 静默重登（后端 401 是空响应，需特殊处理）、统一错误提示
- **图片**：菜品图是阿里云完整链接，直接渲染，无需处理
- **时间**：后端返回 `yyyy-MM-dd HH:mm`，直接显示
- **金额**：统一显示 `¥xx.xx`

## 六、桌码生成方案（每桌一个二维码）

**主方案（推荐）**：用微信官方接口生成"小程序码"（码里带桌号参数），写一个**独立 Java 小工具**（放在后端项目里、不参与后端运行，运行一次生成 1~20 张桌码图片）→ 打印塑封贴桌。开发调试期可在微信开发者工具里用"编译模式"模拟扫码进某号桌；真机扫码需先上传体验版。

**备选方案**：草料等第三方生成普通二维码（内容是网页链接格式）——只适用于小程序正式发布上线之后，开发期没法调试，仅作以后省钱备选。

## 七、实施里程碑（每阶段做完都能验证）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **M0 环境准备**（0.5天） | 确认 MySQL/Redis 已启动；IDEA 跑起后端；浏览器打开 `http://localhost:8080/doc.html` 看到接口文档；微信开发者工具能打开项目 | 接口文档页面可见"用户端接口"分组 |
| **M1 后端补完+接口冒烟**（1天） | 按第四节改 5 处 → 重启后端 → 用接口文档/curl 把"登录→加购→下单(带桌号)→付款→查订单"整条链路走一遍 | 全程无报错；数据库 orders 表出现新订单（状态5已支付、桌号有值）；商家后台收到提醒 |
| **M2 小程序骨架+登录+桌号+人数**（1天） | 清理模板、写全局配置、静默登录、选桌页、人数弹窗、打烊页、点餐页空壳 | 模拟"3号桌"编译直接进点餐页并显示桌号；不扫码则先进选桌页；Redis 改打烊 → 进打烊页 |
| **M3 点餐页+口味+购物车**（2天） | 左分类右菜品联动、口味弹窗、套餐弹窗、购物车球、增删清空 | 同口味加 3 次 → 购物车 1 行×3；不同口味 → 两行；徽标数=总件数 |
| **M4 下单/订单/催单/付款**（1.5天） | 下单确认页、订单详情页、催单、模拟付款、取消 | 下单后购物车清空；付款后状态变"已完成"；商家端收到催单提醒 |
| **M5 历史订单+打磨**（1天） | 历史订单分页/筛选/再来一单、空态加载态、401 静默重登实测 | 再来一单后购物车复现原菜品原口味 |
| **M6 桌码生成+真机联调**（1天） | 生成 20 张桌码打印；上传体验版；手机同一 Wi-Fi 连电脑后端；全流程扫码实测 | 每桌扫码认出正确桌号；手机端全流程跑通；商家端实时收提醒 |
| **M7 上线准备说明**（1天，以说明为主） | 注册正式小程序账号、云服务器+域名+备案+HTTPS、配置合法域名、正式版桌码 | 形成上线操作清单文档 |

**注意**：实现过程中每次你改后端、我改后端或数据库，都需要重启后端才能生效；两边同时改容易打架，建议实现阶段后端改动统一由我列清单、你拍板后执行。

## 八、风险与注意事项

1. **appid/secret 必须一致（最高风险）**：目前项目模板和后端配置用的都是演示 appid `wx888098f8a35aaab4`，开发期可先用它；**正式上线必须换成你自己注册的小程序账号**，并同步改后端配置（secret 只能放后端，严禁写进小程序代码）
2. **Redis/MySQL 未启动**是后端各种异常的常见原因，排障先查这两个
3. 菜单有缓存：商家后台改菜单后小程序可能不显示新菜，重启后端或清 Redis 即可
4. 催单是实时对讲机通知，不存档：商家后台没在线就收不到（属正常现象）
5. 商家端 4 个旧按钮（接单/派送/拒单/统计）点了会报错，属已知遗留问题，不影响本次流程
6. 真机联调三件套：手机电脑同一 Wi-Fi、开发者工具勾选"不校验合法域名"、Windows 防火墙放行 8080 端口

## 九、开工前需要你准备/确认的事

- [ ] 电脑装好**微信开发者工具**，并注册/登录一个小程序账号（或先沿用演示 appid 开发）
- [ ] MySQL、Redis 本地服务已启动（后端依赖它们）
- [ ] 确认自己能启动后端（IDEA 运行），或者实现阶段由我指导你启动
- [ ] 知晓并同意：实现阶段我会小改后端 3 个文件（第四节清单，改前逐条给你过目）

## 十、最终验证方式（端到端）

1. **接口层**（M1）：knife4j 文档页/curl 全链路冒烟，数据库订单记录核对（桌号、状态、金额）
2. **模拟器层**（M2~M5）：微信开发者工具编译模式模拟"扫 3 号桌码"进入，走完整点餐-下单-付款流程
3. **真机层**（M6）：上传体验版，手机扫真实打印的小程序码，从扫码到付款全流程走通；商家后台实时收到"X号桌已下单/已支付/催单"提醒
4. **回归项**：打烊拦截、401 过期自动重登、再来一单、取消订单、空购物车下单拦截
