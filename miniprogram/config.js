// ============================================
// 全局配置：店铺信息、后端地址都集中在这里改
// ============================================
module.exports = {
  // 后端服务地址：
  //   - ✅ 当前：微信云托管（2026-08-20 部署），通过"云调用"内部通道访问
  //   - 云调用（wx.cloud.callContainer）不需要配置合法域名，体验版/正式版都能用
  //   - baseUrl 保留备用：只在开发者工具里直连调试时才用得到（需勾"不校验合法域名"）
  baseUrl: 'https://shaoshaocanting2-300150-11-1472054049.sh.run.tcloudbase.com',

  // ✅ 云调用配置（小程序 → 云托管专属通道，免合法域名）
  cloudEnv: 'prod-d3gj73g1qee76db25',   // 云托管环境 ID（微信公众平台→云开发→环境设置里可查）
  cloudService: 'shaoshaocanting2',     // 服务名：域名开头那段（xxx-数字 之前的部分），服务改名时要同步改这里

  // 店铺信息（商家后台暂时没有店铺设置页，先写在这里）
  shopName: '富有餐馆',
  shopNotice: '欢迎光临，扫码点餐，美味即刻上桌',
  businessHours: '5:00 - 20:00',

  // 店铺背景图与头像图（图片文件放在 miniprogram/images/ 文件夹里）
  shopBg: '/images/shop-bg.jpg',
  shopLogo: '/images/avatar.png',

  // 店里一共有多少张桌子（选桌页用）
  tableCount: 20,

  // 招牌菜分类名：商家后台新建一个叫这个名字的菜品分类，
  // 小程序会自动把它置顶显示（带"荐"标签）
  featuredCategoryName: '招牌菜',

  // 可多选的口味维度名：名单里的口味（如"忌口"），顾客可以同时勾选多个选项
  // （比如"不要葱"+"不要蒜"一起选）；一个都不选 = 默认正常
  // 以后有类似口味，把名字加进这个名单即可
  // ⚠️ 名字必须和商家后台录的口味维度名一字不差
  // ⚠️ 这类口味的选项名里不要出现 + 号（+ 是多选项之间的连接符）
  multiFlavorNames: ['忌口']
};
